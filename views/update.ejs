<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Task</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('navbar') %>
    
    <div class="container">
        <div class="update-card">
            <h1>Update Task Page</h1>
            
            <form action="/update/<%= result._id %>" method="POST">
                <div class="form-group">
                    <input type="text" name="title" value="<%= result.title %>" class="modern-input" placeholder="Task Title">
                </div>

                <div class="form-group">
                    <textarea name="description" class="modern-input" rows="5" placeholder="Description"><%= result.description %></textarea>
                </div>

                <button type="submit" class="modern-btn">Update Task</button>
            </form>
        </div>
    </div>
</body>
</html>